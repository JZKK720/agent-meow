apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: agent-meow-postgres
    component: postgres
spec:
  type: ClusterIP
  selector:
    app: agent-meow-postgres
    component: postgres
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
