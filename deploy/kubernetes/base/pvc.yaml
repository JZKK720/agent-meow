apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agent-meow-artifacts
  labels:
    app: agent-meow
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
