[project]
name = "agent-meow-memory"
version = "0.1.0"
description = "MCP server exposing Hermes agent session memory as tools"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0",
]

[project.scripts]
agent-meow-memory = "agent_meow_memory:_main"
